# dsh-infra

DSH plugin exposing ONE tool `infra(op, target?, params?)` that fronts the Personal Infrastructure Control Plane.

See infra-control/AGENTS.md for the hard constraint (tool count <= 2).

